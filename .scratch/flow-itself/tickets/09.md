## Parent
#1

## What to build
Dogfood: run `flow` on `flow`. Init this repo, create a Run for the work already
done (this spec and these tickets), advance it to its true position, and commit
the resulting `.flow/` so position travels with the branch.

The test of this ticket is not an assertion — it is whether a cold session can
open this repo, run `flow next`, and know what to do without being told.

## Acceptance criteria
- [ ] `.flow/flow.toml` and `.flow/runs/` exist in this repo and are committed
- [ ] A Run exists for this effort, at the Stage it is genuinely on
- [ ] Its Handoff block would orient a session that has never seen this work
- [ ] `.flow/board.html` renders this repo's real state
- [ ] `README.md` explains what flow is, install, and the command surface
- [ ] `flow status` in this repo shows honest state, not a demo fixture

## Blocked by
#9 (ticket 08)
